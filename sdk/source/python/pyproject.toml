[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dsn-source"
version = "0.1.0"
description = "Bounded, best-effort DSN notification producer"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
